<section class="page-section clearfix">
    <div class="container">
        <div class="intro">
            <img class="intro-img img-fluid mb-3 mb-lg-0 rounded" src={{$home1->img}} alt="">
            <div class="intro-text left-0 text-center bg-faded p-5 rounded">
                <h2 class="section-heading mb-4">
                    <span class="section-heading-upper">{{$home1->title}}</span>
                    <span class="section-heading-lower">{{$home1->subtitle}}</span>
                </h2>
                <p class="mb-3">{{$home1->para}}</p>
                <div class="intro-button mx-auto">
                    <a class="btn btn-primary btn-xl" href="#">{{$home1->btn}}</a>
                </div>
            </div>
        </div>
    </div>
</section>
